import { DragDropInputDirective } from './drag-drop-input.directive';

describe('DragDropInputDirective', () => {
  let directive: DragDropInputDirective;

  beforeEach(() => {
    directive = new DragDropInputDirective();
  });

  it('should create', () => {
    expect(directive).toBeTruthy();
  });
});
