import { MainPageComponent } from './main-page.component';

describe('MainPageComponent', () => {
  let component: MainPageComponent;

  beforeEach(() => {
    component = new MainPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
