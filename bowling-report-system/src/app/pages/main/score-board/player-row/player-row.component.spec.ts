import { PlayerRowComponent } from './player-row.component';

describe('PlayerRowComponent', () => {
  let component: PlayerRowComponent;

  beforeEach(() => {
    component = new PlayerRowComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
